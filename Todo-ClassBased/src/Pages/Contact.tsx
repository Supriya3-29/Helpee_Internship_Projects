import { Typography } from 'antd'
import { Component } from 'react'

export default class Contact extends Component {
  render() {
    return (
      <Typography.Title level={1} style={{display:'flex', justifyContent:'center', textAlign:'center', marginTop:'20px'}}>Contact</Typography.Title>
    )
  }
}
